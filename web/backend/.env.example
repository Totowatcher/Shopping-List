# Copy to .env and fill in values.
# Generate a secret: python -c "import secrets; print(secrets.token_hex(32))"
SL_JWT_SECRET=change-me

# Optional overrides
# SL_DATABASE_PATH=./shopping_list.db
# SL_JWT_EXPIRE_MINUTES=525600
# SL_CORS_ORIGINS=http://localhost:5175,http://127.0.0.1:5175
# SL_CORS_LAN_VITE=1
# SL_BOOTSTRAP_USERS=[{"username":"admin","password":"changeme","role":"admin"}]
